# A股智能分析系统 - 增强功能指南

本文档详细介绍系统的增强功能，包括智能选股、大盘复盘、决策仪表盘等模块。

---

## 一、功能概述

本次增强实现了完整的 **选股 → 复盘 → 决策** 工作流：

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  智能选股   │ ──▶ │  大盘复盘   │ ──▶ │  人工筛选   │ ──▶ │  AI 分析    │
│  stock_picker│     │market_analyzer│    │  Web UI    │     │  analyzer   │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
```

### 核心增强点

| 模块 | 功能 | 文件 |
|------|------|------|
| 智能选股 | 基于宏观策略和技术指标的多因子选股 | `stock_picker.py` |
| 市场数据存储 | 持久化大盘、板块、选股数据 | `storage.py` |
| 大盘复盘增强 | 历史对比、北向资金、MA60 判断 | `market_analyzer.py` |
| Web UI V2 | Vue3 + ECharts 可视化决策界面 | `webui_v2.py`, `webui_v2/` |

---

## 二、后台逻辑详解

### 2.1 数据库表结构

新增以下数据表（SQLite，位于 `data/stock_analysis.db`）：

#### `market_daily` - 市场每日数据
```sql
- date              -- 交易日期
- up_count          -- 上涨家数
- down_count        -- 下跌家数
- limit_up_count    -- 涨停数
- limit_down_count  -- 跌停数
- total_amount      -- 两市成交额（亿）
- north_flow        -- 北向资金净流入（亿）
- sh_index          -- 上证指数
- sh_change_pct     -- 上证涨跌幅
- sz_index          -- 深证成指
- cyb_index         -- 创业板指
- sh_ma60           -- 上证60日均线
- sh_close_20d_ago  -- 20日前上证收盘价
- market_state      -- 市场状态: bull/bear/neutral
```

#### `sector_daily` - 板块每日数据
```sql
- date              -- 交易日期
- sector_name       -- 板块名称
- change_pct        -- 涨跌幅
- rank              -- 当日排名
- rank_type         -- top（涨幅榜）/ bottom（跌幅榜）
- leader            -- 领涨股
- leader_change_pct -- 领涨股涨跌幅
- turnover_amt      -- 成交额
```

#### `stock_pool` - 选股池
```sql
- date              -- 选股日期
- stock_code        -- 股票代码
- stock_name        -- 股票名称
- strategy          -- 选股策略
- sector_name       -- 来源板块
- macro_score       -- 宏观评分（0-100）
- tech_score        -- 技术评分（0-100）
- total_score       -- 综合评分
- status            -- pending/selected/dismissed
```

#### `sector_stocks` - 板块成分股映射
```sql
- sector_name       -- 板块名称
- stock_code        -- 股票代码
- stock_name        -- 股票名称
- market            -- SH/SZ
```

#### `policy_analysis` - 政策分析结果
```sql
- date              -- 分析日期
- session           -- pre（盘前）/ post（盘后）
- policy_title      -- 政策标题
- sector_name       -- 受影响板块
- impact_type       -- positive/negative
- impact_score      -- 影响程度（1-5）
- analysis_reason   -- 分析理由
```

### 2.2 选股逻辑

#### 2.2.1 市场状态判断

系统根据以下条件自动判断市场状态：

| 状态 | 条件 | 市值阈值 |
|------|------|----------|
| 牛市 (bull) | 上证 > MA60 且 20日涨幅 > 5% | 80亿 |
| 熊市 (bear) | 上证 < MA60 且 20日跌幅 > 5% | 40亿 |
| 震荡 (neutral) | 其他情况 | 50亿 |

**动态市值阈值**：熊市时放宽标准以捕捉更多小盘机会，牛市时提高标准聚焦优质标的。

#### 2.2.2 宏观层选股策略

| 策略 | 权重 | 说明 |
|------|------|------|
| 政策利好 | 40% | LLM 分析政策新闻，识别受益板块 |
| 热门板块 | 30% | 连续2天涨幅榜前5的板块 |
| 北向资金 | 20% | 近3日北向净流入时，选涨幅榜前10板块 |
| 板块反转 | 10% | 昨日跌幅榜前3，今日转涨的板块 |

#### 2.2.3 技术层筛选策略

| 策略 | 权重 | 条件 |
|------|------|------|
| 多头排列 | 30% | close > MA5 > MA10 > MA20 |
| 放量突破 | 25% | 量比 > 1.5 且 涨幅 > 2% |
| 创新高 | 20% | 收盘价创20日新高 |
| 均线支撑 | 15% | 回踩 MA5/MA10 后企稳 |
| 趋势向上 | 10% | 当日涨幅 > 0 |

#### 2.2.4 选股流程

```python
1. 获取市场状态 → 确定市值阈值
2. 宏观层选股 → 从4个策略获取候选股池
3. 技术层筛选 → 过滤ST/小市值/空头排列股票
4. 综合评分 → macro_score + tech_score
5. 排序取Top N → 保存到 stock_pool 表
```

### 2.3 大盘复盘增强

#### 新增功能

1. **历史数据对比**：自动对比昨日成交额、北向资金变化
2. **MA60 市场状态**：判断指数与60日均线关系
3. **数据持久化**：每日数据存入 `market_daily` 和 `sector_daily`
4. **Prompt 优化**：强制 AI 基于数据事实分析，避免臆测

#### 北向资金获取

```python
# 使用 akshare 获取北向资金
ak.stock_hsgt_north_net_flow_in_em()  # 北向资金净流入
```

---

## 三、Web UI V2 使用指南

### 3.1 页面结构

```
┌────────────────────────────────────────────────────────────┐
│  A股智能分析系统                    选股池 | 大盘复盘 | 决策仪表盘 | 历史记录  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│                      [ 页面内容 ]                          │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 3.2 选股池页面

**功能**：展示智能选股结果，支持人工筛选

| 元素 | 说明 |
|------|------|
| 日期选择器 | 选择查看的选股日期 |
| 执行选股按钮 | 触发当日选股流程 |
| 统计卡片 | 候选股数、已选中、市场状态、市值阈值 |
| 候选股表格 | 代码、名称、策略、板块、评分、涨跌幅、状态 |
| 选择复选框 | 勾选/取消选中股票 |
| 已选列表 | 底部展示已选中的股票，可快速跳转决策仪表盘 |

**操作流程**：
1. 点击「执行选股」获取当日候选股
2. 查看各股票的策略来源、评分、板块信息
3. 勾选感兴趣的股票（状态变为「已选」）
4. 点击「查看决策仪表盘」进入分析页面

### 3.3 大盘复盘页面

**功能**：展示市场全貌，辅助判断大盘环境

| 元素 | 说明 |
|------|------|
| 指数卡片 | 上证、深证、创业板、科创50 的收盘价和涨跌幅 |
| 指数走势图 | 近30日三大指数走势（ECharts） |
| 成交额柱状图 | 近30日两市成交额变化 |
| 板块热力图 | 当日板块涨跌幅可视化（红涨绿跌） |
| 领涨/领跌板块 | 涨跌幅榜前5，含领涨股 |
| 市场统计 | 涨跌家数、涨跌停、成交额、北向资金 |

### 3.4 决策仪表盘页面

**功能**：综合分析已选股票，辅助投资决策

| 元素 | 说明 |
|------|------|
| 市场状态横幅 | 显示当前牛/熊/震荡状态及操作建议 |
| 市场概况卡片 | 指数、成交、涨跌比、北向资金 |
| 选股池概况 | 候选数、已选数，快速跳转链接 |
| 板块动态 | 领涨/领跌板块简要列表 |
| 已选股票分析 | 每只股票的详细卡片（评分、策略、涨跌幅） |
| Top 3 候选 | 综合评分最高的3只候选股 |

### 3.5 历史记录页面

**功能**：查看历史选股和市场数据

| 元素 | 说明 |
|------|------|
| 时间范围选择 | 近7天/14天/30天 |
| 可用日期列表 | 点击切换查看的日期 |
| 选股记录 | 当日选股列表及状态 |
| 大盘数据 | 当日指数、成交额等 |
| 历史概览时间线 | 每日选股数量和已选数量 |

---

## 四、API 接口文档

### 4.1 选股池 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/stock-pool` | GET | 获取选股池列表 |
| `/api/stock-pool/{code}/status` | PUT | 更新股票状态 |
| `/api/stock-pool/selected` | GET | 获取已选股票 |
| `/api/stock-pool/run` | POST | 执行选股 |

### 4.2 市场数据 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/market/today` | GET | 今日大盘数据 |
| `/api/market/history` | GET | 大盘历史数据 |
| `/api/market/state` | GET | 市场状态判断 |

### 4.3 板块数据 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/sectors/today` | GET | 今日板块数据 |
| `/api/sectors/heatmap` | GET | 板块热力图数据 |
| `/api/sectors/{name}/history` | GET | 板块历史数据 |

### 4.4 其他 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/dashboard` | GET | 仪表盘概览数据 |
| `/api/stock/{code}` | GET | 个股数据 |
| `/api/history/stock-pool` | GET | 选股历史 |
| `/api/history/dates` | GET | 可用日期列表 |

---

## 五、本地启动指南

### 5.1 环境准备

```bash
# 1. 安装 Python 依赖
pip install -r requirements.txt

# 2. 安装前端依赖（如需开发模式）
cd webui_v2
npm install
```

### 5.2 启动方式

#### 方式一：全功能命令行模式

```bash
# 执行选股 + 大盘复盘 + 个股分析（完整流程）
python main.py --stock-picker

# 仅执行选股
python main.py --stock-picker --no-notify

# 执行政策分析 + 选股
python main.py --policy-analysis --stock-picker

# 仅执行大盘复盘
python main.py --market-review
```

#### 方式二：Web UI V2 模式（推荐）

```bash
# 启动后端 API（端口 5000）
python main.py --webui-v2

# 或指定端口
python main.py --webui-v2 --webui-v2-port 8080
```

访问 `http://localhost:5000` 即可使用 Web 界面。

#### 方式三：前后端分离开发模式

```bash
# 终端 1：启动后端
python main.py --webui-v2

# 终端 2：启动前端开发服务器（热更新）
cd webui_v2
npm run dev
```

前端访问 `http://localhost:3000`，自动代理 API 请求到后端。

#### 方式四：生产环境部署

```bash
# 构建前端
cd webui_v2
npm run build

# 启动服务（前端静态文件由 Flask 托管）
python main.py --webui-v2
```

### 5.3 推荐的每日使用流程

```bash
# 1. 盘前（9:00 前）：执行政策分析
python main.py --policy-analysis

# 2. 盘后（15:30 后）：执行选股 + 复盘
python main.py --stock-picker --market-review

# 3. 启动 Web UI 进行人工决策
python main.py --webui-v2
```

在 Web UI 中：
1. 进入「选股池」查看候选股
2. 进入「大盘复盘」了解市场环境
3. 勾选感兴趣的股票
4. 进入「决策仪表盘」查看综合分析
5. 根据分析结果做出投资决策

### 5.4 定时任务模式

```bash
# 启动定时任务（每日 15:30 自动执行）
python main.py --schedule --stock-picker
```

---

## 六、配置说明

在 `.env` 文件中配置：

```bash
# 选股配置
STOCK_PICKER_ENABLED=true       # 是否默认启用选股
STOCK_PICKER_TOP_N=10           # 每日最多选股数

# 市值阈值（亿元）
STOCK_PICKER_MV_BULL=80         # 牛市市值阈值
STOCK_PICKER_MV_NEUTRAL=50      # 震荡市值阈值
STOCK_PICKER_MV_BEAR=40         # 熊市市值阈值

# Web UI V2
WEBUI_V2_PORT=5000              # Web UI 端口
```

---

## 七、常见问题

### Q1: 选股结果为空？

检查：
1. 是否已执行大盘复盘（需要 `sector_daily` 数据）
2. 数据库是否有当日板块数据
3. 市值阈值是否过高

### Q2: 北向资金显示为0？

这可能是数据源的实际情况（如节假日或数据延迟）。系统会正确处理。

### Q3: 前端页面空白？

1. 确认后端已启动：`python main.py --webui-v2`
2. 检查端口是否被占用
3. 开发模式下确认 `npm run dev` 正在运行

### Q4: 如何自定义选股策略权重？

修改 `stock_picker.py` 中的常量：

```python
MACRO_WEIGHTS = {
    'policy': 40,
    'hot_sector': 30,
    'north_flow': 20,
    'reversal': 10,
}

TECH_WEIGHTS = {
    'ma_bull': 30,
    'volume_break': 25,
    'new_high': 20,
    'ma_support': 15,
    'trend_up': 10,
}
```

---

## 八、技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Web UI V2                            │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ StockPool│  │ Market  │  │Dashboard│  │ History │        │
│  │   .vue  │  │Review.vue│  │  .vue   │  │  .vue   │        │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘        │
│       │            │            │            │              │
│       └────────────┴────────────┴────────────┘              │
│                         │                                   │
│                    Vue Router                               │
│                         │                                   │
│                    API Client                               │
└─────────────────────────┼───────────────────────────────────┘
                          │ HTTP
┌─────────────────────────┼───────────────────────────────────┐
│                    Flask API                                │
│                   (webui_v2.py)                             │
│                         │                                   │
│  ┌──────────────────────┼──────────────────────┐           │
│  │                 Storage Layer               │           │
│  │                 (storage.py)                │           │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐       │           │
│  │  │ Market  │ │ Sector  │ │  Stock  │       │           │
│  │  │ Daily   │ │ Daily   │ │  Pool   │       │           │
│  │  └─────────┘ └─────────┘ └─────────┘       │           │
│  └─────────────────────────────────────────────┘           │
│                         │                                   │
│  ┌──────────────────────┼──────────────────────┐           │
│  │               Business Logic                │           │
│  │  ┌─────────────┐  ┌─────────────┐          │           │
│  │  │ StockPicker │  │MarketAnalyzer│          │           │
│  │  └─────────────┘  └─────────────┘          │           │
│  └─────────────────────────────────────────────┘           │
│                         │                                   │
│  ┌──────────────────────┼──────────────────────┐           │
│  │                Data Provider                │           │
│  │     AkShare / EFinance / Tushare / ...      │           │
│  └─────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────┘
```

---

*文档更新时间：2026-01-28*
